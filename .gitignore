# OS / editor cruft
.DS_Store
Thumbs.db
.vscode/
*.log

# Never commit secrets — this repo is public
.env
.env.*
*.key
*.pem
secrets.*

# Personal Claude Code permissions (per-machine, never committed)
.claude/settings.local.json
